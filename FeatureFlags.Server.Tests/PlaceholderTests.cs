namespace FeatureFlags.Server.Tests;

// Placeholder until the first vertical slice lands; replace with slice-level integration tests.
public class PlaceholderTests
{
    [Fact]
    public void Placeholder()
    {
        Assert.True(true);
    }
}
